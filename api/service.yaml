apiVersion: v1
kind: Service
metadata:
  name: nextgen-api
spec:
  ipFamilies:
    - IPv4
  ports:
    - name: nextgen-api
      protocol: TCP
      port: 8080
      targetPort: 8080
  internalTrafficPolicy: Cluster
  type: ClusterIP
  ipFamilyPolicy: SingleStack
  selector:
    app: nextgen-api